syntax = "proto3";

package gameserver.rpc;

option csharp_namespace = "GameServer.Rpc";

service GrpcTest {
  rpc Ping (GrpcTestRequest) returns (GrpcTestReply);
}

message GrpcTestRequest {
  string message = 1;
}

message GrpcTestReply {
  string message = 1;
  int64 server_time = 2;
}
